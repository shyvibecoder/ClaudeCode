# Auto-research output

The v3 research engine (`scripts/research-run.mjs`, monthly `research.yml`) writes dated proposal reports here. Each is a HUMAN-APPROVED PR proposing only bot-owned scarcity fields (priced_in/bind_window/non_consensus) — never thesis/tickers. Prompts are versioned in `scripts/lib/research-prompts.mjs`.
